# 下单后返回应用流程与状态说明

## 目标
让用户从外部外卖平台下单回来后，不会断在外部链接，而是继续在应用内完成：

1. 确认取餐点
2. 设置取餐提醒
3. 查看取餐路线
4. 回到当天行程安排

## 核心原则

- 跳转前先记录上下文
- 返回后优先做一件事：确认取餐点
- 每一步都给出完成反馈
- 不让用户重复输入
- 页面承接要轻，不做成复杂表单

## 推荐流程

### 1. 用户点击外部下单
在餐饮页点击 `去美团下单` 时，应用先本地记录：

- `merchantName`
- `title`
- `orderLink`
- `jumpedAt`
- `status = pending_return`

然后再跳转到外部平台。

### 2. 用户离开应用
此时应用把当前 CPS 券卡标记为：

- `已跳转`

如果用户复制过团口令或链接，也分别记录：

- `已复制团口令`
- `已复制链接`

### 3. 用户返回应用
当页面重新获得焦点，且当前存在 `pending_return` 状态时，自动切换为：

- `status = returned`

并显示一张 `回流承接卡`，提示：

- 你已完成外部下单
- 接下来先确认取餐点

### 4. 用户确认取餐点
提供可选取餐点列表，例如：

- `A馆西侧休息区`
- `北门服务台 2号点`
- `B馆摄影区补给点`
- `连廊中转取餐点`

选中后：

- 写入 `pickupPoint`
- 将步骤 `pickup` 标记为已完成
- 在订单追踪区同步显示取餐点

### 5. 用户设置取餐提醒
点击 `去提醒页创建取餐提醒` 后：

- 自动生成提醒草稿
- 跳转到提醒页
- 提醒内容带入商家名与取餐点

建议提醒文案：

- 标题：`取餐提醒`
- 时间：`订单返回后 10 分钟`
- 描述：`真功夫 下单后，提醒你去 A馆西侧休息区 取餐。`

### 6. 用户查看取餐路线
点击 `回地图看路线` 后：

- 带着当前 `pickupPoint` 跳转到地图
- 地图把它识别为 `取餐路线`
- 自动定位目标馆区

### 7. 本次承接完成
当 `pickup / reminder / route` 三步都完成后，可将状态切为：

- `done`

UI 上显示：

- `本次取餐安排已完成`
- `系统会按提醒时间提示你去取餐`

## 状态机建议

### CPS 主状态

- `idle`：未开始
- `pending_return`：已跳转，等待返回
- `returned`：已返回应用
- `done`：承接完成

### 回流步骤状态

- `pickup`：确认取餐点
- `reminder`：设置取餐提醒
- `route`：查看取餐路线

每个步骤都包含：

- `key`
- `label`
- `done`

## 本地存储建议

建议用一个本地对象维护：

```json
{
  "merchantName": "真功夫",
  "title": "冬菇鸡蒸蛋汤套餐兑换券",
  "orderLink": "http://dpurl.cn/3kLbuKrz",
  "status": "returned",
  "jumped": true,
  "returned": true,
  "copiedType": "command",
  "pickupPoint": "A馆西侧休息区",
  "nextSteps": [
    { "key": "pickup", "label": "确认取餐点", "done": true },
    { "key": "reminder", "label": "设置取餐提醒", "done": false },
    { "key": "route", "label": "回地图确认路线", "done": false }
  ]
}
```

## 页面承接建议

### 餐饮页
负责：

- 下单入口
- CPS 券卡状态
- 回流承接主面板
- 取餐点确认

### 提醒页
负责：

- 展示取餐提醒草稿
- 统一查看当天提醒

### 地图页
负责：

- 显示取餐点目标馆区
- 提供推荐路线

## 视觉反馈建议

- 已复制：轻标签提示
- 已跳转：状态 tag
- 已返回：成功状态 tag
- 已确认取餐点：步骤勾选
- 已设置提醒：步骤勾选
- 已查看路线：步骤勾选

## 当前 demo 建议优先级

1. 自动识别用户返回应用
2. 取餐提醒真正同步到提醒页
3. 订单追踪显示当前取餐点
4. 地图真正识别取餐目标

这四步做完，整条链路就能从“外链跳转”升级成“应用内闭环承接”。
